<h1>Edit Student Information</h1>
 
<%= form_for :exams, url: exam_path(@exams), method: :patch do |f| %>
 
  <% if @exams.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@exams.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @exams.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
 
  <p>
    <%= f.label :student_number %><br>
    <%= f.text_area :student_number %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', exams_path %>
